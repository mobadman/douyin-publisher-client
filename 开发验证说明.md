# 短视频批量发布助手 3.0.6 开发验证说明

3.0.6 沿用3.0.5已经验证的Windows文件选择、上传、封面、定时和单次点击发布链路。仅调整平台明确返回“发布成功”后的处理：不再进入作品管理做45秒即时硬核验，直接继续下一条；作品ID仍在批次完成后通过现有“同步视频ID”功能核验。页面仅跳转、超时或出现模糊结果时仍会停止，禁止自动重复点击发布。旧版安装包及源码快照分别保留在对应版本目录，用于回档。

## 工作区

软件预置三个发布工作区：抖音主页号、抖音商城号、微信视频号。三个工作区分别保存飞书表格链接、本地素材库、下载缓存、计划和发布日志。抖音主页号与抖音商城号共用同一个发布账号及 Chrome Profile；微信视频号使用自己的发布账号及 Profile。切换工作区前必须关闭当前平台与飞书 Chrome。

飞书链接支持 `/sheets/…?sheet=…`、`/wiki/…?sheet=…`，以及不带 `sheet` 参数的 `/wiki/<知识库节点token>`。只接受飞书官方 HTTPS 域名；电子表格直链需要保留 `sheet` 参数，知识库表格节点直链不要求该参数。

生成计划时，软件把工作区、发布账号和发布配置写入计划快照。执行时再次核对计划绑定的账号，禁止拿其他工作区账号执行。

## 商城号

商城工作区在常规封面、文案、Tag之外，还要求：

- `商品短标题库/产品型号.txt`：每行一个短标题，生成计划时固定到具体视频。
- `商品配置/产品型号映射.csv`：维护“飞书产品型号,抖店搜索型号”。第一列必须与飞书【产品型号】完全对应，第二列填写抖店实际检索使用的产品代码型号。
- 商城商品查询复用抖音发布账号的 Chrome 登录环境，只读打开商品管理页、现场搜索型号并复制候选链接，不编辑抖店商品。

软件不维护固定商品链接库。每次生成商城计划后，必须点击“现场获取本批商品链接”；候选链接只保存在当前计划中。异常占位价会排除；正常商品同价时按库存、销量排序；正常价格不同时必须在计划编辑窗口人工选择。商城计划默认每条挂车，缺少短标题或当批已确认链接时不允许发布。

抖音主页号与商城号共用账号，因此排期使用两条互斥分钟轨道：主页号只使用分钟尾数 `00/10/20/30/40/50`，商城号只使用 `05/15/25/35/45/55`。即使两个工作区选择同一天，发布时间也不会重合。

## 视频号

视频号使用独立 Chrome Profile。发布入口按“视频号助手首页 → 可见的发表视频按钮 → 发布表单 iframe”处理。合集、视频标注和原创声明保持平台默认值，软件不修改。测试工具页通过平台下拉选项在“抖音测试”和“微信视频号测试”之间切换，两者使用独立测试 Profile。

遇管理员实名扫码验证时，当前视频进入“等待管理员扫码”状态；验证完成后继续。等待超时会停止当前执行并把该条标记为可续发失败，不清空已完成记录，也不重新执行整批。视频号发布 ID/公开链接接口已预留，当前不主动获取。

## AI标识与ID表格

飞书【AI标识】为可选列。勾选或填写 `TRUE、是、AI、AI生成、内容由AI生成、1、yes、√、☑` 时，该计划项显示“内容由AI生成”，抖音发布时在封面完成后进入【自主声明】，选择【内容由AI生成】并确认。未标记行不进入此步骤；微信视频号链路不受影响。

“更新ID记录”会同时生成TXT和CSV。两者包含【所在行、视频名称、发布ID、视频网址、获取状态】，并按飞书实际行号升序排列。“复制ID表格”只复制已经获取的【发布ID】，每行一个纯数字，不含“发布ID”表头，不复制空值，可直接粘贴到飞书或Excel的单列区域。

## 验证顺序

3.0.6 在进入生产前必须按以下顺序验证：

1. 安装后分别切换三个工作区，确认各自飞书链接和素材目录互不串用，并确认主页号与商城号显示同一个发布账号。
2. 分别保存一个 `/sheets/` 链接和一个 `/wiki/` 链接，检测飞书登录并验证实际拉取。
3. 商城号先测试产品型号映射、抖店现场搜索与候选选择，不发布；用一组同价商品和一组不同价商品验证规则。
4. 商城号使用小号或可删除的定时视频做单条挂车测试，核对商品链接和短标题，再测试中断续发。
5. 在统一测试工具中切换到视频号，验证登录、账号检测、单条上传、定时、封面和管理员扫码，再测试两条批次续发。
6. 同一天分别生成主页号和商城号计划，确认两组发布时间没有任何相同分钟。
7. 抖音主页号对照 2.0.2 做单条回归，确认标准分支没有出现行为变化。
8. 用一条勾选【AI标识】和一条未勾选的视频生成计划，确认只有前者执行自主声明；同步ID后检查TXT、CSV和粘贴结果均按飞书实际行排序。
9. 使用至少两条定时视频验证连续发布：每条出现平台“发布成功”后应直接进入下一条，不应跳转作品管理等待45秒；批次完成后再执行“同步视频ID”。

任一真实页面链路未通过前，不应把 3.0.6 替换为生产版本。
